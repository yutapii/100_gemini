# Gemini CLI運用設定

## 概要

100_gemini システムで使用する Gemini CLI の運用設定・使用方法をまとめる。

---

## システム情報

**システム名**: 100_gemini
**役割**: Claude Code 苦手科目担当（点検・検索・YouTube等）
**ポート**: 5100
**管理元**: STP (010_STP_APIv1)

---

## Gemini CLI基本情報

**バージョン**: 0.27.0
**インストール場所**: `/Users/saitoyutaka/001_npm-global/lib/node_modules/@google/gemini-cli`
**設定ファイル**: `~/.gemini/settings.json`
**認証方式**: OAuth Personal

---

## デフォルトモデル

**推奨モデル**: `gemini-2.5-flash`

**理由**:
- 高速処理
- コスト効率が良い
- コード点検・セキュリティ診断に十分な精度
- クォータ制限が緩い

**Pro を使うべき場合**:
- 非常に複雑な推論が必要
- Flash では精度不足
- クォータに余裕がある

---

## 設定ファイル

### settings.json

**パス**: `/Users/saitoyutaka/.gemini/settings.json`

**内容**:
```json
{
  "model": {
    "name": "gemini-2.5-flash"
  },
  "auth": {
    "method": "oauth-personal"
  },
  "security": {
    "auth": {
      "selectedType": "oauth-personal"
    }
  }
}
```

**バックアップ**:
```bash
cp ~/.gemini/settings.json ~/.gemini/settings.json.backup
```

---

## コマンドオプション

### 基本コマンド

```bash
# 対話モード起動
gemini

# プロンプト指定（非対話）
gemini -p "プロンプト内容"

# モデル指定（一時的）
gemini -p "プロンプト" --model gemini-2.5-flash

# ヘルプ表示
gemini --help
```

### セッション内コマンド

```bash
# モデル切り替え
/model

# 設定表示
/settings

# ヘルプ
/help
```

---

## 使用例

### 1. コード点検

```bash
gemini -p "以下のファイルのセキュリティ診断と品質チェックをお願いします：
/Users/saitoyutaka/100_gemini/index.html

チェック項目：
1. XSS脆弱性
2. HTMLの構造・セマンティクス
3. アクセシビリティ
4. パフォーマンス
5. セキュリティベストプラクティス

厳しめに評価してください。"
```

### 2. ファイル内容確認

```bash
gemini -p "以下のファイルの内容を要約してください：
/Users/saitoyutaka/100_gemini/010_ai/CLAUDE.md"
```

### 3. バグ分析

```bash
gemini -p "以下のエラーの原因を分析してください：
[エラーメッセージ貼り付け]

考えられる原因と対策を教えてください。"
```

### 4. ドキュメント生成

```bash
gemini -p "以下のコードに対するREADME.mdを作成してください：
[コード貼り付け]

マークダウン形式でお願いします。"
```

---

## モデル切り替え

### 対話式切り替え（推奨）

```bash
$ gemini
> /model

╭───────────────────────────────────────────────────────╮
│ Select Model                                           │
│                                                        │
│   1. Auto (Gemini 2.5)                                │
│      Let Gemini CLI decide the best model              │
│ ● 2. Manual (gemini-2.5-pro)                          │
│      Manually select a model                           │
│                                                        │
│ Remember model for future sessions: false              │
│ (Press Tab to toggle)                                  │
╰───────────────────────────────────────────────────────╯

# Tab キーで「Remember model for future sessions」を true に設定
# Enter で確定
```

### 設定ファイル直接編集

```bash
# バックアップ
cp ~/.gemini/settings.json ~/.gemini/settings.json.backup

# 編集
vi ~/.gemini/settings.json

# "name": "gemini-2.5-pro" → "gemini-2.5-flash" に変更

# 確認
cat ~/.gemini/settings.json | grep "name"
```

---

## STPパネル連携（Phase 6実装予定）

**予定機能**:
- STPパネル（http://localhost:5030/）にモデル切り替えスイッチ追加
- GUIでワンクリック切り替え
- 現在のモデルをリアルタイム表示

**APIエンドポイント**（予定）:
```
GET  /api/gemini/model       - 現在のモデル取得
POST /api/gemini/model       - モデル変更
```

---

## トラブルシューティング

### クォータ枯渇エラー

```
TerminalQuotaError: You have exhausted your capacity on this model.
Your quota will reset after 20hXXmXXs.
```

**対策**:
1. モデルを Flash に切り替え
2. クォータリセットを待つ（約20時間）

詳細: `080_tps/gemini_quota_management.md`

### 認証エラー

```
Authentication failed
```

**対策**:
1. OAuth再認証
   ```bash
   gemini --login
   ```

2. API キー確認
   ```bash
   cat ~/.gemini/.env
   ```

### モデル切り替えが反映されない

**対策**:
1. settings.json 確認
   ```bash
   cat ~/.gemini/settings.json
   ```

2. Gemini CLI再起動
   ```bash
   gemini -p "テスト：1+1は？"
   ```

---

## ベストプラクティス

### ✅ 推奨

- デフォルトは Flash モデル
- 長いプロンプトは `-p` オプションで渡す
- 結果は 020_work_reports/ に保存
- 重要な設定変更前にバックアップ

### ❌ 非推奨

- Pro モデルの連続使用（クォータ枯渇リスク）
- 設定ファイルの直接編集（typoリスク）
- 認証情報の公開リポジトリへのコミット

---

## 関連ドキュメント

**100_gemini/**:
- `010_ai/CLAUDE.md` - AI設定・起動の作法
- `080_tps/gemini_quota_management.md` - クォータ管理
- `020_work_reports/` - 作業報告・点検結果

**STP（010_STP_APIv1/）**:
- `110_panel/` - 統合管理パネル
- `000_concept/NEW_SYSTEM_CHECKIN_GUIDE.md` - 新規システム導入ガイド

**外部リソース**:
- [Gemini CLI 公式ドキュメント](https://github.com/google/generative-ai-js/tree/main/packages/cli)
- [Google AI Studio](https://aistudio.google.com/)

---

## 更新履歴

| 日付 | 変更内容 | 担当 |
|------|---------|------|
| 2026-02-04 | 初版作成、クォータ対応記録 | STP🔧 |

---

**作成日**: 2026-02-04
**管理**: STP🔧 / 100_gemini
**次回レビュー**: Phase 6実装後（STPパネルスイッチ追加）
