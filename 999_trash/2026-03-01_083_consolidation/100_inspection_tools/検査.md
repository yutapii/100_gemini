# 鋼鉄の関門 v3.4：汎用品質検査キット (Iron Gate Inspection Kit)

**版数**: v3.4 (2026-02-28)
**設計思想**: 規律・安全・保守の三位一体。
「100点以外リリース禁止」の徹底。

---

## 1. 検査対象と構造 (1機能1番号)

```text
[ルート]/
├── 000_concept/   # 設計
├── 010_ai/        # AI
├── 030_config/    # 共通設定
├── 040_security/  # 機密 ※.gitignore
├── [番号]_機能/    # 各機能
└── 999_trash/     # 廃止
```

---

## 2. 規律検査基準 (Discipline)

「物理的制約」を1バイトの妥協なく遵守せよ。

| 項目 | 制限 | 判定 | 備考 |
| :--- | :--- | :--- | :--- |
| **行数** | **500内** | `wc -l` | 接近で機能分解せよ。 |
| **一行** | **80内** | `awk` | 可読性担保。 |
| **名** | **蛇腹** | `[a-z_]` | 互換性重視。 |

---

## 3. セキュリティ検査基準 (Security)

「漏洩は死」を徹底点検。

- **.gitignore**: `040_security/` 除外。
- **ハード排除**: `password`, `key` 等。
- **パス排除**: `$HOME` 等の依存。
  - `Path(__file__)` 等を使用。

---

## 4. メンテナンス性検査基準 (Maintainability)

「設計の品格」。

- **設定分離**: JSON/YAML分離。
- **storage禁止**: API経由。
- **冪等性**: 何回実行しても安全か。
- **証跡**: `.log` に記録。

---

## 5. 検査ツール (自動化)
... Applied fuzzy match at line 4-62.
以下のコマンドで一括検査を実行せよ。

```bash
# プロジェクトルートで実行
./100_inspection_tools/inspect_v3_iron_gate.sh [対象ディレクトリ]
```

---

## 6. 敵対的レビュアーの心得

検査担当（Gemini）は、常に「意地悪な視点」を持て。
1. あと10行追加で規律が壊れないか？
2. 設定のハードコードは怠慢ではないか？
3. このパスは別のPCでエラーにならないか？

**「ざる検査」は最大の裏切りである。**

gemini検査専属AI です。
